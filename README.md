# vision_grid_test

Note: do not forget, to create local env file or populate existed one.
Project has fixtures file if you need.

To run docker:
 ```
    docker-compose up --build
 ```

Project is running under - http://localhost:8080/

Calculation api - POST /api/shipping/calculate
    
